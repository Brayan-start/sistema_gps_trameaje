import { Routes, Route, Navigate } from "react-router-dom";import ProtectedRoute from "./router/ProtectedRoute";import Login from "./pages/Login";import AdminDashboard from "./pages/AdminDashboard";import DriverDashboard from "./pages/DriverDashboard";import PublicMap from "./pages/PublicMap";import History from "./pages/History";import Reports from "./pages/Reports";export default function App() {  return (    <Routes>      <Route path="/login" element={<Login />} />      <Route path="/mapa-publico" element={<PublicMap />} />      <Route        path="/dashboard"        element={          <ProtectedRoute role="admin">            <AdminDashboard />          </ProtectedRoute>        }      />      <Route        path="/dashboard/*"        element={          <ProtectedRoute role="admin">            <AdminDashboard />          </ProtectedRoute>        }      />      <Route        path="/chofer"        element={          <ProtectedRoute role="driver">            <DriverDashboard />          </ProtectedRoute>        }      />      <Route        path="/historial"        element={          <ProtectedRoute role={["admin", "driver"]}>            <History />          </ProtectedRoute>        }      />      <Route        path="/reportes"        element={          <ProtectedRoute role="admin">            <Reports />          </ProtectedRoute>        }      />      <Route path="/" element={<Navigate to="/mapa-publico" replace />} />      <Route path="*" element={<Navigate to="/mapa-publico" replace />} />    </Routes>  );}