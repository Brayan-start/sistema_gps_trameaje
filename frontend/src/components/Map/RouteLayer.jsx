import { Polyline, Tooltip, Marker } from "react-leaflet";import L from "leaflet";export default function RouteLayer({ route, showLabel = false }) {  if (!route?.geometry?.coordinates) return null;  const positions = route.geometry.coordinates.map(([lng, lat]) => [lat, lng]);  const midpoint = positions.length > 0    ? positions[Math.floor(positions.length / 2)]    : null;  return (    <>      <Polyline        positions={positions}        pathOptions={{          color: "#22d3ee",          weight: 4,          opacity: 0.5,          dashArray: "12, 8",        }}      />      <Polyline        positions={positions}        pathOptions={{          color: "#22d3ee",          weight: 2,          opacity: 0.8,        }}      />      {positions.length > 0 && (        <Polyline          positions={[positions[0], positions[positions.length - 1]]}          pathOptions={{ opacity: 0 }}        >          <Tooltip permanent direction="center">            <span style={{ color: "#22d3ee", fontWeight: 600, fontSize: 12 }}>              {showLabel ? "Ruta destinada: San Roque → Ceja" : "Ruta San Roque → Ceja"}            </span>          </Tooltip>        </Polyline>      )}    </>  );}