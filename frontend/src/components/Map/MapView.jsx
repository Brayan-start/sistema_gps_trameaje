import { MapContainer, TileLayer } from "react-leaflet";const CENTER = [-16.5, -68.19];const ZOOM = 13;export default function MapView({ children, center = CENTER, zoom = ZOOM, className = "" }) {  return (    <MapContainer      center={center}      zoom={zoom}      className={`h-full w-full ${className}`}      zoomControl={false}      attributionControl={false}    >      <TileLayer        url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"        attribution="&copy; OpenStreetMap contributors"      />      {children}    </MapContainer>  );}