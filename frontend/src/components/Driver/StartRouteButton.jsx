export default function StartRouteButton({ active, onStart, onStop }) {  return (    <div className="absolute bottom-6 left-0 right-0 z-[1000] flex justify-center px-6">      {active ? (        <button          onClick={onStop}          className="w-full max-w-xs py-4 rounded-2xl bg-alert text-white font-bold text-lg shadow-lg shadow-alert/30 active:scale-95 transition"        >          TERMINAR RUTA        </button>      ) : (        <button          onClick={onStart}          className="w-full max-w-xs py-4 rounded-2xl bg-green-500 text-white font-bold text-lg shadow-lg shadow-green-500/30 active:scale-95 transition animate-pulse-border"        >          INICIAR RUTA        </button>      )}    </div>  );}