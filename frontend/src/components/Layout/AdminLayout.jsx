import { useState } from "react";import Sidebar from "./Sidebar";import { useAuthStore } from "../../store/authStore";import ChangePasswordModal from "../ChangePasswordModal";const API = import.meta.env.VITE_API_URL || "";export default function AdminLayout({ activeTab, onTabChange, children }) {  const [collapsed, setCollapsed] = useState(false);  const [showMiCuenta, setShowMiCuenta] = useState(false);  const user = useAuthStore((s) => s.user);  const logout = useAuthStore((s) => s.logout);  return (    <div className="h-screen w-screen flex bg-dark-bg overflow-hidden">      <Sidebar        activeTab={activeTab}        onTabChange={onTabChange}        collapsed={collapsed}        onToggle={() => setCollapsed(!collapsed)}        userName={user?.name}      />      <div className="flex-1 flex flex-col min-w-0">        <header className="h-14 bg-dark-card border-b border-dark-border flex items-center justify-between px-4 shrink-0">          <div className="flex items-center gap-3">            <button              onClick={() => setCollapsed(!collapsed)}              className="text-gray-400 hover:text-white p-1 rounded-lg hover:bg-dark-hover transition"            >              <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>                <path strokeLinecap="round" strokeLinejoin="round" d="M4 6h16M4 12h16M4 18h16" />              </svg>            </button>          </div>          <div className="flex items-center gap-2">            <button              onClick={() => setShowMiCuenta(true)}              className="flex items-center gap-1.5 text-xs text-gray-500 hover:text-accent transition px-2 py-1.5 rounded-lg hover:bg-dark-hover"            >              <svg className="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>                <path strokeLinecap="round" strokeLinejoin="round" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z" />              </svg>              Mi cuenta            </button>            <span className="text-xs text-gray-500 hidden sm:block">              {user?.role === "admin" ? "Administrador" : "Conductor"}            </span>            <button              onClick={() => { logout(); window.location.href = "/login"; }}              className="flex items-center gap-1.5 text-xs text-gray-500 hover:text-alert transition px-2 py-1.5 rounded-lg hover:bg-dark-hover"            >              <svg className="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>                <path strokeLinecap="round" strokeLinejoin="round" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />              </svg>              Salir            </button>          </div>        </header>        <main className="flex-1 overflow-hidden">          {children}        </main>      </div>      {showMiCuenta && (        <ChangePasswordModal          endpoint={`${API}/admin/cambiar-contrasena`}          onClose={() => setShowMiCuenta(false)}        />      )}    </div>  );}