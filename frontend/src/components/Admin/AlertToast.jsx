import { useState, useEffect, useCallback } from "react";export default function AlertToast({ incidentes, onAplicarSanction, onDismiss }) {  return (    <div className="fixed top-4 right-4 z-[5000] flex flex-col gap-2 max-w-sm">      {incidentes.map((inc) => (        <div          key={inc.incidentId}          className="card p-4 border-alert/50 shadow-modal animate-slide-in-right flex items-start gap-3"        >          <div className="w-8 h-8 rounded-full bg-alert/20 flex items-center justify-center shrink-0 mt-0.5">            <span className="text-lg">⚠️</span>          </div>          <div className="flex-1 min-w-0">            <p className="text-sm font-semibold text-white">              {inc.driver_name || "Chofer"} salió de la ruta            </p>            <p className="text-xs text-gray-500 mt-0.5">              {new Date(inc.timestamp).toLocaleString("es-BO")}            </p>            <div className="flex gap-2 mt-2">              <button                onClick={() => onAplicarSanction(inc)}                className="px-3 py-1.5 rounded-lg bg-accent text-[#0b1121] text-xs font-semibold hover:bg-accent-400 transition"              >                Aplicar sanción              </button>              <button                onClick={() => onDismiss(inc.incidentId)}                className="px-3 py-1.5 rounded-lg text-xs text-gray-500 hover:text-white transition"              >                Cerrar              </button>            </div>          </div>        </div>      ))}    </div>  );}