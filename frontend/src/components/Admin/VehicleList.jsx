import { useState, useEffect } from "react";import { useAuthStore } from "../../store/authStore";const API = import.meta.env.VITE_API_URL || "";export default function VehicleList({ vehicles, setVehicles }) {  const [allVehicles, setAllVehicles] = useState([]);  const [loading, setLoading] = useState(true);  const token = useAuthStore((s) => s.token);  useEffect(() => {    fetch(`${API}/vehicles`, {      headers: { Authorization: `Bearer ${token}` },    })      .then((r) => r.json())      .then((data) => {        setAllVehicles(data);        if (vehicles.length === 0) setVehicles(data);      })      .catch(console.error)      .finally(() => setLoading(false));  }, []);  const statusColor = (status) => {    switch (status) {      case "on_route": return "bg-green-500";      case "stopped": return "bg-yellow-500";      case "active": return "bg-accent";      default: return "bg-gray-500";    }  };  const statusLabel = (status) => {    switch (status) {      case "on_route": return "En ruta";      case "stopped": return "Detenido";      case "active": return "Activo";      default: return "Inactivo";    }  };  if (loading) {    return (      <div className="h-full flex items-center justify-center">        <div className="text-gray-400">Cargando vehículos...</div>      </div>    );  }  return (    <div className="h-full overflow-y-auto p-4">      <h2 className="text-sm font-semibold text-gray-300 mb-3">Vehículos</h2>      <div className="space-y-2">        {allVehicles.map((v) => (          <div            key={v.id}            className="flex items-center justify-between px-4 py-3 rounded-lg bg-[#1e293b]"          >            <div className="flex items-center gap-3">              <div className={`w-2.5 h-2.5 rounded-full ${statusColor(v.status)}`} />              <div>                <p className="text-sm font-medium">{v.plate}</p>                <p className="text-xs text-gray-400">                  {v.brand} {v.model} · {v.year}                </p>              </div>            </div>            <div className="text-right">              <span className="text-xs text-gray-400 block">{statusLabel(v.status)}</span>              {v.driver_name && (                <span className="text-xs text-accent">{v.driver_name}</span>              )}            </div>          </div>        ))}      </div>      {allVehicles.length === 0 && (        <p className="text-center text-gray-500 mt-8">No hay vehículos registrados</p>      )}    </div>  );}